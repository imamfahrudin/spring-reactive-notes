package com.imamfahrudin.notes;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Integration test for SpringReactiveNotesApplication to verify that the application context loads successfully.
 */
@SpringBootTest
class SpringReactiveNotesApplicationTest {

    /**
     * Test that the application context loads without errors.
     * This is a basic smoke test to ensure Spring Boot configuration is correct.
     */
    @Test
    void contextLoads() {
        // If this test passes, the application context loaded successfully
        // This verifies that all beans are properly configured and there are no circular dependencies
    }
}